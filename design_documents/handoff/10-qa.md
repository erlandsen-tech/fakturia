# 10 — QA checklist + handoff close

## Visual regression checklist

Run through each surface after implementing. Compare side-by-side with the relevant artboard in the source `Fakturio Brand.html` (use a second tab, no need to recreate from memory).

### Tokens & type
- [ ] `bg-paper` resolves to `oklch(0.946 0.028 78)` in computed styles (≈ #F4ECDC)
- [ ] `text-clay` is `oklch(0.626 0.171 38)` (≈ #DC4F2C)
- [ ] `<h1 className="font-display">` renders Instrument Serif italic-capable (test: `<em>` should look genuinely italic, not synthesized)
- [ ] `<span className="font-mono">kr 12 480,00</span>` shows tabular figures (test: vertically align two amounts of different lengths — the digits should line up)
- [ ] `.cap` class produces 11px / 0.16em / uppercase

### Landing
- [ ] Hero illustrations don't overlap CTAs at `lg` breakpoint
- [ ] Manifesto strip is on `bg-ink` and italic em is `text-sun`
- [ ] Three values: middle column has `bg-paper-2` background tint
- [ ] MCP terminal block uses `bg-ink` + `font-mono` and reads correctly
- [ ] Pricing: "Best for de fleste" badge appears on the middle (Vekst) tier only

### Public invoice (`/i/[token]`)
- [ ] No navbar visible (chrome hidden via the pathname guard in chapter 04)
- [ ] InkwellTide illustration appears at `lg` breakpoint and degrades gracefully on mobile
- [ ] Big amount is `font-mono` at 38px
- [ ] "Vipps", "Kort", "PDF" buttons are 56px tall (size="lg")
- [ ] Bank-transfer side rail is `bg-ink` text-paper
- [ ] Note block has the clay left border + italic serif

### Dashboard
- [ ] Greeting includes user's first name in italic serif
- [ ] StatCards use the correct top-border color per accent (overdue=clay-red, paid=sage-green, ink for neutral)
- [ ] Status pills use `border-status-*` with matching text color
- [ ] Row hover bg is `paper-2`
- [ ] Mono numerals align in the amount column

### PDF
- [ ] Generated PDF preserves Instrument Serif italics in "Faktura" + "Å betale NOK" + the note block
- [ ] All amounts use JetBrains Mono with 2 decimal places
- [ ] Background is the `D6CDB7` paper-edge with the inner `FAF6ED` page

### Email
- [ ] Renders correctly in Gmail web (Outlook can be best-effort)
- [ ] The inline-SVG mark shows in clients that allow inline SVG; falls back gracefully where they don't (the layout still works without it)
- [ ] CTA button is `#DC4F2C` solid, white text

## Acceptance: light pass

```bash
npm run lint
npm run build
```

Both must pass. The build also catches missing imports between the new brand component files.

## Manual smoke test

```bash
npm run dev
```

1. Visit `/` — confirm hero, manifesto strip, three values, MCP block, pricing
2. Visit `/dashboard` (signed in) — confirm restyled stats + table
3. Visit `/invoices/create` — confirm paper card + AI sidebar
4. Visit `/i/<a-real-share-token>` — confirm public invoice
5. Click "Last ned PDF" — confirm the brand PDF downloads

## Known gaps (out of scope, surface in next sprint)

1. **Reverse-rendering the email in dark-mode inboxes.** Currently optimised for light. Apple Mail's auto-dark inverts colors well enough; Outlook does not. Acceptable for v1.
2. **Empty-state illustrations** for dashboard (no invoices yet) — pick one of the 5 + a one-line ink caption.
3. **Sign-in / sign-up screens** — the brand doc didn't define these. Recommend a simple paper card centered on `bg-paper-grain`, italic serif heading, single email field, clay submit button. Defer.
4. **Settings page rebrand** — same surgical-restyle approach as Dashboard. Defer.
5. **Sidebar layout option** for `/dashboard` and `/invoices` (240px ink rail) — present in the brand doc but the existing topnav works. Add as a Tweak in v2.

## Final commit (rollup)

If you're squashing, the high-level commit message:

```
feat(brand): port Fakturio paper/clay/ink identity end-to-end

- foundations: rewrite globals.css tokens (OKLCH), swap fonts to
  Instrument Serif + Inter Tight + JetBrains Mono, add bg-paper-grain
- atoms: Wordmark, Mark, button clay variant, navbar rebrand
- illustrations: 5 surreal SVG components (ReceiptBird, CitrusCalc,
  StampMoth, CoinStones, InkwellTide)
- landing: full rewrite to brand-led layout with MCP/AI band
- public: new /i/[token] recipient view with Vipps/card/PDF CTAs
- dashboard: restyled stats + table with brand chrome
- create-invoice: paper card form + AI assist sidebar
- pdf+email: restyled InvoicePDF + transactional HTML email
- i18n: nb-NO primary, en parallel, server-side t() helper

Visual ground truth: design/Fakturio Brand.html
```

That's it. Ship it.
